<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PracticeApplicationBRG" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customized theme here -->
        <item name="android:windowSplashScreenBackground">@color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/brstart</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/white</item>
        <item name="android:windowSplashScreenAnimationDuration">300</item>
    </style>

    <!-- Main application theme -->
    <style name="Theme.PracticeApplicationBRG" parent="Base.Theme.PracticeApplicationBRG" />
</resources>